# Supabase has been removed — data now lives in Cloudflare D1 (see wrangler.jsonc).
# Admin auth secrets (ADMIN_PASSWORD_HASH, SESSION_SECRET) go in .dev.vars locally
# (gitignored) and as `wrangler secret put` values in production — never in this file.
